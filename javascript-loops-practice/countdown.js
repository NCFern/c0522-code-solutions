/* exported countdown */
function countdown(number) {
  var numberArray = []; // create array for output //
  var currentNumber = numberArray;// makes our number equal our current number//
  for (var i = currentNumber; i > 0; i--) {
    if (currentNumber === 0);
  }
}
