/* exported getStudentNames */
// access array for student.name at start index[0]//
// push student.name[0] into output into array//
// go back to start. keep going until i < student.name.length //

function getStudentNames(students) {
  var finalStudents = [];
  for (var i = 0; i < students.length; i++); // use for...in loop //

  return finalStudents;
}
