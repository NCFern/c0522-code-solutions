/* exported addSuffixToAll */
// grab string from words array [i] //
// add suffix//
// push string to array //
// loop back to string//
// stop once i < words.length //

function addSuffixToAll(words, suffix) {
  var finalWords = [];
  for (var i = 0; i < words.length; i++);
  // finalWords.push({words[i] + suffix}); //

  return finalWords;
}
