/* exported getLastChar */
// write a function that gets the last character of the string //
// create a var that gets the character at the last string//
// return output //

function getLastChar(string) {
  var lastChar = string.charAt(string.length - 1);
  return lastChar;
}
