/* exported firstChars */

// give the length of characters in the string //
// slice string from 0 to the length //

function firstChars(length, string) {
  var numberedString = string.slice(0, length);
  return numberedString;
}
